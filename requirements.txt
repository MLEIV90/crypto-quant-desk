pandas>=2.0
numpy>=1.24
requests>=2.31
pyarrow>=14.0
pytest>=7.4
statsmodels>=0.14
scipy>=1.10
matplotlib>=3.7
jupyter>=1.0
nbconvert>=7.0
ipykernel>=6.0
arch>=6.3
scikit-learn>=1.3
xgboost>=2.0
PySide6>=6.5
mplfinance>=0.12
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
reportlab>=4.0

# Fase 18 (experimento de investigación, Deep RL para asignación de
# cartera — ver rl/). PESO: torch instala una wheel de ~120-700MB según
# plataforma (CPU-only en este proyecto, no se pidió CUDA) — es, con
# diferencia, la dependencia más pesada del repo. Si el tamaño de la
# instalación es un problema (CI, disco, contenedores chicos), estas tres
# líneas son las únicas que dependen de eso; el resto de la app (API,
# frontend, ML supervisado, GARCH, backtests) funciona sin ellas.
gymnasium>=0.29
stable-baselines3>=2.3
torch>=2.2
